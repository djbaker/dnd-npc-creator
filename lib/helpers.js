let Promise = require('bluebird');
let angular = require('angular');

module.exports = {
  getClassData: function (class, level, next) {
    return new Promise(function (resolve, reject) {

    });
  }







};